exports.getTransactionId = (amount) => {
    //1. generate a unique disbursementId - 
    //order status - keep the disbursement
}

function createDisbursement() {
    //use the 'OD'year-month-day-hour-second-milisecond-(4-6) digts of random number
    return 
}